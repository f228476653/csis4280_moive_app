<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movieapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".service.ServiceBuilder"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activities.DetailActivity"></activity>
        <activity android:name=".ui.activities.SearchActivity" />
        <activity android:name=".ui.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.RegisterActivity" />
        <activity android:name=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.NewMessageActivity" />
        <activity android:name=".ui.activities.AddPostActivity" />
        <activity android:name=".ui.activities.PostActivity" />
        <activity android:name=".ui.activities.ProfileActivity" />
        <activity android:name=".ui.activities.DirectMessageActivity" />
        <activity android:name=".ui.activities.HeaderMessageActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
    </application>

</manifest>